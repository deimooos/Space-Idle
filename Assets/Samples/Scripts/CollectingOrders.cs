using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollectingOrders : MonoBehaviour
{/*
    // Start is called before the first frame update
    void Start()
    {
        
    }
    private void OnTriggerEnter(Collider other)
    {
        collectOrder();
        gameObject.SetActive(false);
    }
    // Update is called once per frame
    void Update()
    {
        
    }
    void collectOrder()
    {
        Deliver.scr.isLanded = false;
        GlobalSettings.scr.steelCount += Order.scr.steelCount;
        GlobalSettings.scr.springCount += Order.scr.springCount;
        GlobalSettings.scr.gasolineCount += Order.scr.gasolineCount;
        GlobalSettings.scr.wrenchCount += Order.scr.wrenchCount;
        GlobalSettings.scr.boltsCount += Order.scr.boltsCount;
        GlobalSettings.scr.gearCount += Order.scr.gearCount;
         Order.scr.steelCount = 0;
         Order.scr.springCount = 0;
         Order.scr.gasolineCount= 0;
         Order.scr.wrenchCount = 0;
         Order.scr.boltsCount = 0;
         Order.scr.gearCount = 0;
    }
}
*/
}